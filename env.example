AWS_ZONE_ID=your_aws_zone_id          # The route53 zone ID for the domain
AWS_DOMAIN_NAME=example.com           # The domain name to check A records for and update them if necessary
# Optional. Default level is INFO
LOG_LEVEL=DEBUG